"""
Módulo `core` — lógica de cálculo pura, sem dependências de Streamlit.

Toda função aqui deve ser testável isoladamente via pytest.
A separação Streamlit (UI) ↔ core (cálculo) permite reuso do código
em scripts CLI, notebooks ou eventual API.
"""
